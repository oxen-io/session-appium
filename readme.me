nvm install # use node version from the .nvmrc file here
nvm use # same
yarn install --frozen # install the exact version of packages from .lock file
yarn add -g appium@next # install appium2 globally